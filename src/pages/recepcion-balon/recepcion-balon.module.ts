import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RecepcionBalonPage } from './recepcion-balon';

@NgModule({
  declarations: [
    RecepcionBalonPage,
  ],
  imports: [
    IonicPageModule.forChild(RecepcionBalonPage),
  ],
})
export class RecepcionBalonPageModule {}
