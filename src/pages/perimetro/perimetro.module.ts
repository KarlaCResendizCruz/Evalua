import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PerimetroPage } from './perimetro';

@NgModule({
  declarations: [
    PerimetroPage,
  ],
  imports: [
    IonicPageModule.forChild(PerimetroPage),
  ],
})
export class PerimetroPageModule {}
